/*
var is_scrappy = confirm( "When Scrappy was introduced..." );

if ( is_scrappy ) {
	console.log( "Scooby Doo jumped the shark" );
} else {
	console.log( "Scooby Doo was an epic cartoon" );
}
*/

/*
var eggs = 1;
var dozen = 12;


while( eggs <= dozen ){
	var is_halfDozen = confirm( "Is " + eggs +" a half dozen?" );

	if( is_halfDozen ){
		console.log( "You got it" );
	} else {
		console.log( "Let me guess again..." );
	}

	eggs = eggs + 1;
}
*/