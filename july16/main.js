/*
var user_answer = prompt( "Your name, please:" );

console.log( user_answer );


function myfunction( str, num )
{
	console.log( str + " : " + num );
}
*/

/*
function promptUserForAge()
{
	var user_age = prompt( "What is your age?" );
	console.log( user_age );

	while( user_age < 0 || typeof( user_age ) == "string" ) 
	{
		if( user_age >= 30 )
		{
			document.body.style.backgroundColor = 'grey';
			break;
		} else if( user_age <= 33 && user_age > 0 ) {
			document.write( "Look at the young buck!!!" );
			break;
		}
	}
	return false;
}
*/

function handleUserLogin(  )
{

	var js_username = document.getElementById( "username" );
	var js_password = document.getElementById( "password" );

	console.log( js_username + " : " + js_password );
	return false;

}
